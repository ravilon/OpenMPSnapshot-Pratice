#pragma once

/**
 * @brief Cast rays for a normal 2D projection (either orthographic or perspective), but multiple layers are stored with surfaces entered and exited until stop condition is met.
 * 
 */